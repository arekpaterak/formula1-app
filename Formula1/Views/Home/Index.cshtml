@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="standings-header">Welcome!</h1>

    <form asp-action="Index" method="post">
        <div align="center">
            <button type="submit" class="btn btn-primary">Sign in</button>
        </div>
    </form>
</div>
